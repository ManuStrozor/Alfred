name: Setup Node
description: Configure Node.js (avec cache npm) et installe les dépendances via npm ci.
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: npm
    - run: npm ci
      shell: bash
